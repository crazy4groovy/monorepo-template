# firebase-auth Environment Variables
# Copy to .env in your app or service and fill with values from Firebase Console
# https://console.firebase.google.com → Project Settings → General

# =============================================================================
# CLIENT (web apps: React, Svelte, Astro)
# Use VITE_ prefix for Vite apps (React, Svelte) or PUBLIC_ prefix for Astro
# =============================================================================

# Firebase API key (Firebase Console → Project Settings → Your apps)
VITE_FIREBASE_API_KEY=AIza...
# For Astro, use: PUBLIC_FIREBASE_API_KEY=AIza...

# Auth domain, typically: your-project-id.firebaseapp.com
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# Firebase project ID
VITE_FIREBASE_PROJECT_ID=your-project-id

# Firebase app ID (Firebase Console → Project Settings → Your apps)
VITE_FIREBASE_APP_ID=1:123456789:web:abc123

# Optional: Auth emulator for local testing (e.g. 127.0.0.1:9099)
# VITE_FIREBASE_EMULATOR_HOST=127.0.0.1:9099

# =============================================================================
# SERVER (Express, Node.js backends)
# =============================================================================

# Firebase project ID (same as client)
FIREBASE_PROJECT_ID=your-project-id

# Path to service account JSON (Firebase Console → Project Settings → Service accounts)
# Generate a new private key and save the file path here
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Alternative: base64-encoded service account JSON (for serverless, e.g. Vercel, Railway)
# FIREBASE_SERVICE_ACCOUNT_JSON=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50Ii...

# Optional: Auth emulator for local testing
# FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099

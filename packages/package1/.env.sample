# Package1 Environment Variables
# Copy this file to .env and update with your actual values
# Note: This is a library package, env vars are mainly for testing

# Default currency code for formatting (default: USD)
DEFAULT_CURRENCY=USD

# Default locale for number formatting (default: en-US)
DEFAULT_LOCALE=en-US